import pickle

# pickle_filename = 'xanax.pickle'

# past_pairs = pickle.loads(open(pickle_filename, "rb").read())

# print(past_pairs)

# past_pairs['annie'] = ['aileen']


# open(pickle_filename, "wb").write(pickle.dumps(past_pairs))